import { createElement } from '../utils/dom-utils.js';import{createPrimaryButton}from'../components/primary-button.js';import{MESSAGES}from'../data/messages.js';
export function createCompletedScreen({onHome}={}){const root=createElement('section',{className:'screen screen--completed'});const icon=createElement('div',{className:'success-icon',text:'✓',attributes:{'aria-hidden':'true'}});const title=createElement('h1',{text:MESSAGES.completed.title,attributes:{tabindex:'-1'}});const message=createElement('p',{className:'completed-message',text:MESSAGES.completed.congratulations});const button=createPrimaryButton({label:MESSAGES.completed.backHome,onActivate:onHome});root.append(icon,title,message,button.element);return{element:root,focusTarget:title,destroy(){button.destroy();root.remove();}}}
